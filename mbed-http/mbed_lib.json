{
    "name": "mbed-http",
    "config": {
        "http-buffer-size": {
            "help": "Size of the HTTP receive buffer in bytes",
            "value": 1024,
            "macro_name": "HTTP_RECEIVE_BUFFER_SIZE"
        }
    }
}
